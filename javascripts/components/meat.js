const meatObject = {
    ham: .50,
    turkey: .45,
    roastBeef: .60,
    chicken: .35
}

const addMeat = () => {
    // console.log(meatObject);
    return meatObject;
};

export default { addMeat };